# Add consumer ProGuard rules here for the domain module if needed.
# Typically, pure Kotlin/Java modules might not need extensive ProGuard rules unless they use reflection
# or have specific entry points that need to be preserved when used by an Android app. 