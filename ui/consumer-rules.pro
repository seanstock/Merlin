# Add consumer ProGuard rules here for the ui module.
# These rules will be applied to any app that includes this module.
# If using reflection or other dynamic features in UI components, rules might be needed.
# Jetpack Compose itself is generally handled well by R8, but custom components might need rules. 